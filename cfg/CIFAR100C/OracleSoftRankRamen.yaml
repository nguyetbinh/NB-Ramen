# The planned preregistered sweep is gamma: 0, 0.25, 0.5, 1.0.  Set gamma in
# a copied run config; gamma: 0 must recover CausalRamen retrieval exactly.
max_capacity: 750
capacity_scope: per_class
topk: 5
beta: 5.0
gamma: 0.5
include_current: true
oracle_context_source: evaluator_domain_idx

optimizer: signsgd
lr: 0.01

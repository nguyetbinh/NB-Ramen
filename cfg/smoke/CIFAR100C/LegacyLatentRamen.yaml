# Cost-limited mechanics smoke only; not a full benchmark configuration.
max_capacity: 4
topk: 2
beta: 5.0
spawn_threshold: 0.25
max_contexts: 8
router_temperature: 0.1
router_momentum: null

optimizer: signsgd
lr: 0.01
